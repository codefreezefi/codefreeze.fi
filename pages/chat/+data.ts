import type { PageData } from '../../renderer/documentProps.ts'

export const data = (): PageData => ({
	layout: 'page',
	title: 'Codefreeze · Chat',
	description:
		'From 2023 on we use the open and distributed communication protocol Matrix for our chat.',
})
